<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="booking.mapper.ExampleMapper">

    <select id="selectAll" resultType="booking.entity.Example">
        SELECT exam_char exam FROM example;
    </select>
</mapper>